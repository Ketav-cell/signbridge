# WebSocket URL for the Python inference server.
# Default: ws://localhost:8000/ws (works for local development).
# Change this if you deploy the inference server elsewhere.
NEXT_PUBLIC_INFERENCE_WS_URL=ws://localhost:8000/ws
